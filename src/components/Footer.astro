---
import business from "../data/business.json";

const links = [
  { name: "Inicio", href: "#inicio" },
  { name: "Nosotros", href: "#nosotros" },
  { name: "Menú", href: "#menu" },
  { name: "Galería", href: "#galeria" },
  { name: "Contacto", href: "#contacto" },
];

const socials = [
  { name: "Instagram", icon: "📸", url: "https://instagram.com" },
  { name: "Facebook", icon: "💬", url: "https://facebook.com" },
  { name: "TikTok", icon: "🎵", url: "https://tiktok.com" },
];
---

<footer
  class="bg-[#1a1a1a] text-white py-16 relative overflow-hidden"
  style={`border-top: 4px solid ${business.colorPrimary}`}
>
  <!-- Decorativo de fondo -->
  <div
    class="absolute inset-0 bg-gradient-to-t from-[var(--primary-color)]/10 via-transparent to-transparent pointer-events-none"
  ></div>

  <div class="max-w-6xl mx-auto px-6 md:px-12 relative z-10">
    <!-- Sección principal -->
    <div
      class="flex flex-col md:flex-row justify-between items-center md:items-start gap-10 mb-10"
    >
      <!-- Logo / Nombre -->
      <div class="text-center md:text-left">
        <h3
          class="text-2xl font-bold mb-3 tracking-wide"
          style={`color:${business.colorAccent}`}
        >
          {business.name}
        </h3>
        <p class="text-gray-300 max-w-sm">
          {business.description}
        </p>
      </div>

      <!-- Navegación -->
      <nav class="flex flex-col md:flex-row gap-4 text-gray-300 text-sm">
        {links.map((link) => (
          <a
            href={link.href}
            class="hover:text-[var(--primary-color)] transition-colors"
          >
            {link.name}
          </a>
        ))}
      </nav>

      <!-- Redes sociales -->
      <div class="flex gap-5 text-2xl">
        {socials.map((s) => (
          <a
            href={s.url}
            target="_blank"
            class="hover:scale-110 transition-transform"
            title={s.name}
          >
            {s.icon}
          </a>
        ))}
      </div>
    </div>

    <!-- Línea divisoria -->
    <div class="border-t border-white/20 my-6"></div>

    <!-- Créditos -->
    <div class="text-center text-gray-400 text-sm">
      © {new Date().getFullYear()} {business.name}. Todos los derechos reservados.
      <br />
      Desarrollado con ☕ y 💻 por{" "}
      <a
        href="https://andescode.com"
        target="_blank"
        class="text-[var(--primary-color)] hover:underline"
      >
        AndesCode
      </a>
      .
    </div>
  </div>
</footer>

<style is:global>
  :root {
    --primary-color: {business.colorPrimary};
    --accent-color: {business.colorAccent};
  }
</style>
